version: 2

stack_defaults:
    before_init:
        - echo "waaaaaaah"    
        - ls -la

stacks:
    onboard1:
        project_root: chaws
        terraform_version: 1.5.7
    compute-storage:
        project_root: compute-storage
        terraform_version: 1.5.7
    database:
        project_root: database
        terraform_version: 1.5.7