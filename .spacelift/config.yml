version: 2

stacks:
    onboard1:
        project_root: chaws
        terraform_version: 1.5.7
    compute-storage:
        project_root: compute-storage
        terraform_version: 1.5.7
    database:
        project_root: database
        terraform_version: 1.5.7

stack_defaults:
    before_init:
        - echo "waaaaaaah"    
        - ls -la
